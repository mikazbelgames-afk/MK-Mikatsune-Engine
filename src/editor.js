import{Engine,fileToDataURL,loadImage}from'./core.js';import{idle,blink}from'./animator.js';const q=s=>document.querySelector(s),engine=new Engine(q('#stage'));let idleOn=true,blinkStart=null;function renderLayers(){q('#layers').innerHTML='';[...engine.layers].reverse().forEach(l=>{const d=document.createElement('div');d.className='layer '+(l.id===engine.selectedId?'active':'');d.textContent=l.name+' · '+l.role;d.onclick=()=>{engine.selectedId=l.id;renderLayers();sync()};q('#layers').appendChild(d)})}function sync(){const l=engine.selected;if(!l)return;for(const k of['x','y','scale','rotation','pivotX','pivotY'])q('#'+k).value=l[k];q('#role').value=l.role}for(const k of['x','y','scale','rotation','pivotX','pivotY'])q('#'+k).addEventListener('input',e=>{const l=engine.selected;if(!l)return;l[k]=Number(e.target.value);l.base=engine.snapshot(l)});q('#role').addEventListener('change',e=>{const l=engine.selected;if(!l)return;l.role=e.target.value;renderLayers()});q('#add').onclick=()=>q('#file').click();q('#file').addEventListener('change',async e=>{const f=e.target.files?.[0];if(!f)return;const src=await fileToDataURL(f),image=await loadImage(src);engine.addLayer({name:f.name.replace(/\.[^.]+$/,''),src,image});renderLayers();sync();e.target.value=''});q('#save').onclick=()=>{const b=new Blob([JSON.stringify(engine.serialize(),null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(b);a.download='mikatsune-project.json';a.click();URL.revokeObjectURL(a.href)};q('#load').onclick=()=>q('#loadFile').click();q('#loadFile').addEventListener('change',async e=>{const f=e.target.files?.[0];if(!f)return;await engine.load(JSON.parse(await f.text()));renderLayers();sync()});q('#idle').onclick=()=>{idleOn=!idleOn;q('#idle').textContent=idleOn?'⏸ Idle':'▶ Idle'};q('#blink').onclick=()=>blinkStart=performance.now();q('#reset').onclick=()=>{for(const l of engine.layers){Object.assign(l,l.base);l.runtime={}}sync()};function frame(now){if(idleOn)idle(engine,now/1000);if(blinkStart!==null){const p=(now-blinkStart)/220;if(p>=1)blinkStart=null;else blink(engine,p)}engine.draw();requestAnimationFrame(frame)}renderLayers();requestAnimationFrame(frame)